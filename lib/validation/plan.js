const Errors = require('./errors');

// Shared validator for plans.

module.exports = ({name, contact, email, address, phone}) => {
    const errors = new Errors();
    // TODO finish.
    return errors;
};
